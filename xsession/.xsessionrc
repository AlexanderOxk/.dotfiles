if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi

if [ -d "$HOME/.local/rust/cargo/bin" ] ; then
    PATH="$HOME/.local/rust/cargo/bin:$PATH"
fi
export PATH
